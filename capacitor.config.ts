import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.atr.inventorymanager',
  appName: 'ATR Store Inventory Manager',
  webDir: 'dist'
};

export default config;
